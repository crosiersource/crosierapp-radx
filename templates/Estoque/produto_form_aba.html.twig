<div class="tab-pane fade" id="{{ aba|lower|transliterate }}" role="tabpanel"
     aria-labelledby="{{ aba|lower|transliterate }}-tab">

    <div class="container" style="margin-bottom: 50px">


        {% for campo in camposNaAba %}
            {{ form_row(form.jsonData[campo]) }}
        {% endfor %}


        <div class="row mt-3">
            <div class="col text-right">
                <button type="submit" class="btn btn-primary" value="Salvar">
                    <i class="fas fa-save" aria-hidden="true"></i> Salvar
                </button>
            </div>
        </div>


    </div>


</div>